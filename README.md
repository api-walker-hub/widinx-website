# WidinX Regulation OS — clickable mockup

Single-file clickable prototype of the merged WidinX UX (WidinX DPP console × Regulation OS v2.9).
Served as a DigitalOcean App Platform static site from `public/index.html`.

Update: replace `public/index.html`, commit, push this `mockup` branch, then trigger a deployment.
